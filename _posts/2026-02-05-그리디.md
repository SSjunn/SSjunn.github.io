---
title: "그리디 알고리즘[Greedy]"
date: 2026-02-05
categories: [코딩테스트]
tags: [그리디]
---
# 그리디 알고리즘(탐욕법, Greedy)

💡 그리디 알고리즘
**정의**
- 최적의 값을 구해야 하는 상황에서 사용되는 근시안적인 방법론으로 '각 단계에서 최적이라고 생각되는 것을 선택' 해 나가는 방식으로 진행하여 최종적인 해답에 도달하는 알고리즘
- 항상 최적의 값을 보장하는 것이 아니라 최적의 값의 '근사값'을 목표

**예시 사진**
<img width="763" height="389" alt="image" src="https://github.com/user-attachments/assets/65b73a8c-3e50-4b7e-8422-99a5cf3bf713" />

💡 그리디 알고리즘 주요 속성
**탐욕 선택 속성(Greedy Choice Property)**

- 각 단계에서 '최선의 선택'을 했을 때 전체 문제에 대한 최적해를 구할 수 있는 경우
- 각 단계에서 가장 이상적인 선택을 하는것이 전체적으로 최적의 결과를 가져옴
- 앞의 선택이 이후의 선택에 영향을 미치면 안됌

**최적 부분 구조(Optimal SubStructure)** 
- 전체 문제의 최적해가 '부분 문제의 최적해로 구성'될 수 있는 경우
- 전체 문제를 작은 부분 문제로 나누어 각각의 부분 문제에서 최적의 해를 구한 후 이를 조합하여 전체 문제의 최적해를 구하는 것을 의미

---

💡 언제 사용해야 하는가?

1. 최적해 보장
   - 위 2가지의 조건이 성립되지 않는다면 그리디 알고리즘으로 최적해를 구할 수 없음
   - 최적의 결과를 구해야 하는 문제라면 조건이 성립하는지를 판단
   - ex) 거스름돈 문제(동전의 종류가 제약되는 등...)
2. 적당한 결과
   - 최적이 아니더라도, 어떻게든 결과만 맞게 나오면 되는 경우라면 그리디를 사용
3. 근사치
   - 그리디로 적당히 나온 결과를 기준으로 복잡한 알고리즘에서의 역할을 수행

-  내가 생각하는 사용하는 순간
   - 가장 좋아 보이는 선택을 하는 순간을 이야기 할 때 거스름돈, 회의실 등등 최적의 선택을 할 때
   - 선택의 기준이 하나의 수치로 되어있어 정렬 기준이 1개로 깔끔할 때
   - 반례가 있으면 백트래킹/DP 없으면 그리디
      - 지금의 선택보다 조금 손해를 감수하면 나중에 큰 이득을 본다 라는 전제

---

# 그리디,DP,백트래킹 코드 구조 차이

1. 그리디(Greedy)
**특징** : 매 순간 최적의 선택을 하고, 돌아가지 않음

```java
public class Greedy {
    
    // 기본 구조
    public int greedy(int[][] items) {
        int result = 0;
        
        // 1. 정렬 (보통 필요)
        Arrays.sort(items, (a, b) -> b[1] - a[1]); // value 기준 내림차순
        
        // 2. 순차적으로 최선의 선택
        for (int[] item : items) {
            if (canSelect(item)) {  // 선택 가능하면
                result += item[1];  // 바로 선택 (되돌리지 않음)
            }
        }
        
        return result;
    }
}
// 반환값이 음수 : a,b 순서 유지, 양수 : b,a로 순서 변경
Arrays.sort(items, (a, b) -> 비교결과);
Integer[] arr = {5, 2, 8, 1};

// 오름차순
Arrays.sort(arr, (a, b) -> a - b);
// 결과: [1, 2, 5, 8]

// 내림차순
Arrays.sort(arr, (a, b) -> b - a);
// 결과: [8, 5, 2, 1]
```
2. DP(Dynamic Programming)
특징 : 메모이제이션 테이블에 부분 문제 결과 저장 -> 재사용<br>
&#8251; 메모이제이션 : 이미 계산한 부분 문제의 결과를 저장해 두고, 다시 필요할 때 그대로 재사용하는 기법

**Bottom-up (반복문)**

```java
public class DPBottomUp {
    
    public int solution(int n) {
        // 1. 테이블 초기화
        int[] dp = new int[n + 1];
        dp[0] = 0;  // base case
        dp[1] = 1;  // base case
        
        // 2. 작은 문제부터 순차적으로 해결
        for (int i = 2; i <= n; i++) {
            // 점화식: 이전 결과를 활용
            dp[i] = dp[i - 1] + dp[i - 2];
        }
        
        return dp[n];
    }
}
```

**Top-Down방식(재귀 + 메모이제이션)**

```java
public class DPTopDown {
    
    private int[] memo;
    
    public int solution(int n) {
        memo = new int[n + 1];
        Arrays.fill(memo, -1);
        return dp(n);
    }
    
    private int dp(int n) {
        // 1. 기저 조건
        if (n <= 1) return n;
        
        // 2. 이미 계산했으면 재사용
        if (memo[n] != -1) return memo[n];
        
        // 3. 계산 후 저장
        memo[n] = dp(n - 1) + dp(n - 2);
        return memo[n];
    }
}
```
3. 백트래킹 (BackTracking)
**특징** : 재귀로 탐색하거나 조건 불만족 시 되돌아감

```java
public class Backtracking {
    
    private List<List<Integer>> result = new ArrayList<>();
    
    public List<List<Integer>> solution(int[] nums) {
        backtrack(nums, new ArrayList<>(), new boolean[nums.length]);
        return result;
    }
    
    private void backtrack(int[] nums, List<Integer> current, boolean[] visited) {
        // 1. 종료 조건 (정답 찾음)
        if (current.size() == nums.length) {
            result.add(new ArrayList<>(current));  // 깊은 복사!
            return;
        }
        
        // 2. 가능한 모든 선택지 탐색
        for (int i = 0; i < nums.length; i++) {
            // 3. 가지치기 (유망하지 않으면 스킵)
            if (visited[i]) continue;
            
            // 4. 선택
            visited[i] = true;
            current.add(nums[i]);
            
            // 5. 재귀 탐색
            backtrack(nums, current, visited);
            
            // 6. 선택 취소 (백트래킹의 핵심!)
            current.remove(current.size() - 1);
            visited[i] = false;
        }
    }
}
```
### 정리

```java
// 그리디: 정렬 → 순회하며 조건 맞으면 바로 선택

Arrays.sort(arr);
for (item : arr) {
    if (조건) result += item;  // 되돌리지 않음
}


// DP: 배열 선언 → 점화식으로 채우기
int[] dp = new int[n + 1];
for (int i = 1; i <= n; i++) {
    dp[i] = dp[i-1] + 무언가;  // 이전 값 재사용
}



// 백트래킹: 선택 → 재귀 → 선택 취소
void backtrack(상태) {
    if (종료조건) { 결과저장; return; }
    
    for (선택지) {
        if (가지치기) continue;
        선택();
        backtrack(다음상태);
        선택취소();  // ← 핵심!
    }
}
```

----

### 문제 설명
점심시간에 도둑이 들어, 일부 학생이 체육복을 도난당했습니다. 다행히 여벌 체육복이 있는 학생이 이들에게 체육복을 빌려주려 합니다. 학생들의 번호는 체격 순으로 매겨져 있어, 바로 앞번호의 학생이나 바로 뒷번호의 학생에게만 체육복을 빌려줄 수 있습니다. 예를 들어, 4번 학생은 3번 학생이나 5번 학생에게만 체육복을 빌려줄 수 있습니다. 체육복이 없으면 수업을 들을 수 없기 때문에 체육복을 적절히 빌려 최대한 많은 학생이 체육수업을 들어야 합니다.
전체 학생의 수 n, 체육복을 도난당한 학생들의 번호가 담긴 배열 lost, 여벌의 체육복을 가져온 학생들의 번호가 담긴 배열 reserve가 매개변수로 주어질 때, 체육수업을 들을 수 있는 학생의 최댓값을 return 하도록 solution 함수를 작성해 주세요.

### 제한사항
전체 학생의 수는 2명 이상 30명 이하입니다.
체육복을 도난당한 학생의 수는 1명 이상 n명 이하이고 중복되는 번호는 없습니다. 여벌의 체육복을 가져온 학생의 수는 1명 이상 n명 이하이고 중복되는 번호는 없습니다.여벌 체육복이 있는 학생만 다른 학생에게 체육복을 빌려줄 수 있습니다. 여벌 체육복을 가져온 학생이 체육복을 도난당했을 수 있습니다. 이때 이 학생은 체육복을 하나만 도난당했다고 가정하며, 남은 체육복이 하나이기에 다른 학생에게는 체육복을 빌려줄 수 없습니다.

### 생각
- 여벌 체육복을 가져온 학생의 번호를 오름차순으로 정렬
- 체육복을 읽어 버린 애들 빌려주는거 계산
- 내가 내옷으로 해결하는거 계산
- 
```java
import java.util.*;

class Solution {
    public int solution(int n, int[] lost, int[] reserve) {
        int answer = n - lost.length;

        Arrays.sort(lost);
        Arrays.sort(reserve);

        // 자기 여벌로 해결
        for (int i = 0; i < lost.length; i++) {
            for (int j = 0; j < reserve.length; j++) {
                if (lost[i] == reserve[j]) {
                    answer++;
                    lost[i] = -1;      // lost도 처리
                    reserve[j] = -1;   // 빌려줄 수 없음 표시
                    break;
                }
            }
        }

        // 친구에게 빌리기
        for (int i = 0; i < lost.length; i++) {
            if (lost[i] == -1) continue; // 이미 해결된 학생 스킵 <- 요고를 생가가도 못했네

            for (int j = 0; j < reserve.length; j++) {
                if (reserve[j] == lost[i] - 1 || reserve[j] == lost[i] + 1) {
                    answer++;
                    reserve[j] = -1;
                    break;
                }
            }
        }

        return answer; 
    }
}

```














